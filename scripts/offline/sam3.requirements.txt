# Core runtime for the local SAM3 backend on Windows.
# Use the Windows-specific Triton build so the offline bundle carries the
# package that exposes the `triton` module on Windows.
torch>=2.4.0
torchvision>=0.19.0
triton-windows; platform_system == "Windows"
numpy>=1.26,<3
opencv-python>=4.10.0.84
Pillow>=10.4.0
requests>=2.32.3,<2.34
timm>=1.0.17
tqdm
ftfy==6.1.1
regex
iopath>=0.1.10
pycocotools
psutil
einops
typing_extensions
huggingface_hub
